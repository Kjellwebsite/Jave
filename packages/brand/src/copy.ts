/** Brand copy rendered into assets. Change here, then re-render. */
export const BRAND_NAME = 'JAVELIN';

/** The motto uses a typographic apostrophe (U+2019), matching server settings. */
export const BRAND_MOTTO = 'YOU THINK YOU’RE ELITE? PROVE IT.';

/**
 * The motto as it is set in assets: the challenge, then the demand, on two
 * lines. Joined with a space, the lines are exactly `BRAND_MOTTO` (tested).
 */
export const BRAND_MOTTO_LINES = ['YOU THINK YOU’RE ELITE?', 'PROVE IT.'] as const;
